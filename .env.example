# PostgreSQL Database Connection URL (matches default docker-compose setup)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/support_ticket?schema=public

# JWT Authentication Secret Key
JWT_SECRET=replace-with-a-secure-secret-key-in-production

# Server Configuration
PORT=4000
NODE_ENV=development
